# Copy to .env for local dev (never commit .env)
AI_API_KEY=YOUR_API_KEY
# Vision model for image attachments (Gizmo chat photos, schedule scanner)
# freetokenfaucet.com slugs: mimo-v2.5 (Standard) or mimo-v2.5-pro (Pro)
# Android Profile → AI Settings picks Standard/Pro for vision chats only.
AI_MODEL=mimo-v2.5
# Text-only chat and study tools (summarize, flashcards, quiz, study-plan)
TEXT_MODEL=auto
AI_BASE_URL=https://freetokenfaucet.com/v1
PORT=8080

# Render (production): set these in Dashboard → studentai-backend → Environment
#   AI_API_KEY     = your freetokenfaucet.com key (Secret, sync: false in render.yaml)
#   AI_MODEL       = mimo-v2.5 or mimo-v2.5-pro (vision default)
#   TEXT_MODEL     = auto (text-only chat and study tools)
#   AI_BASE_URL    = https://freetokenfaucet.com/v1
# PORT is injected automatically by Render — do not set it manually.
# The Android app calls the Render backend URL only — never freetokenfaucet.com directly.
